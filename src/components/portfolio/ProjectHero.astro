---
interface Props {
  class?: string;
  [key: string]: unknown;
}

const { class: className, ...attributes } = Astro.props;
---

<section class:list={['project-hero', className]} {...attributes}>
  <slot />
</section>
