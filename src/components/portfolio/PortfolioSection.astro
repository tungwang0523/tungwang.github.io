---
interface Props {
  class?: string;
  [key: string]: unknown;
}

const { class: className, ...attributes } = Astro.props;
---

<section class:list={['portfolio-section', className]} {...attributes}>
  <slot />
</section>
