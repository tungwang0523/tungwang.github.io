---
interface Props {
  class?: string;
  [key: string]: unknown;
}

const { class: className, ...attributes } = Astro.props;
---

<figure class:list={['project-figure', className]} {...attributes}>
  <slot />
</figure>
